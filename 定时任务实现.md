---
title: 定时任务实现
category: ccnubox
type: deep-dive
topic: middleware
module: bff
frameworks:
  - go-kratos
status: seedling
tags:
  - ccnubox
  - ccnubox/common
  - programming-language/go/cron
---

# 定时任务（cron）使用文档

项目中使用的底层库为 [[robfig cron 的使用]]。

## 1. 总览

- 底层库统一使用 **`github.com/robfig/cron/v3` v3.0.1**（`be-class/go.mod:21`、`be-elecprice/go.mod:12`、`be-grade/go.mod:17` 等）。
- **没有** K8s CronJob、Linux crontab、xxl-job 之类的外部调度；所有定时任务都是**进程内**调度。
- 项目内并存两种封装：
  - **主线 A：直接用 `robfig/cron/v3`**（5 处）
  - **主线 B：项目自封装 `common/pkg/cronx`**（2 处）
  - **伪 cron：`time.Ticker` + `for { select }`**（3 处），名字叫 cron 但并未用 cron 库

| 服务 / 包 | 实际实现 | 封装层 |
|---|---|---|
| `bff` | robfig/cron | `common/pkg/cronx` |
| `be-classlist` | robfig/cron | `common/pkg/cronx` |
| `be-class` | robfig/cron | 直接使用 |
| `be-proxy` | robfig/cron（含 `WithSeconds`） | 直接使用 |
| `common/bizpkg/proxy` | robfig/cron | 直接使用 |
| `be-content` | **ticker** | `cron` 包（伪） |
| `be-elecprice` | **ticker** | `cron` 包（伪） |
| `be-feed` | **ticker** | `cron` 包（伪） |

## 2. 主线 B：`common/pkg/cronx` 项目封装

文件：`common/pkg/cronx/cron.go`（230 行）。

### 2.1 能力

- `Manager{AddTask/RemoveTask/ListTasks/Stop}` 增删查 + 生命周期管理。
- `TaskFunc` 统一签名：`func(ctx context.Context, log logger.Logger)`，所有任务拿到带 `task_id/spec` 字段的 logger。
- 自动 `recover()` 防止单个任务 panic 拖垮调度器。
- 启动 / 结束 / panic / duration 均通过 `common/pkg/logger` 输出结构化日志。
- 错误哨兵：`ErrTaskExists / ErrTaskNotFound / ErrInvalidTaskID / ErrNilTaskFunc`。
- 扩展口：`WithContext(ctx)` / `WithCronOptions(opts...)`（当前未使用）。

### 2.2 任务签名

```go
// common/pkg/cronx/cron.go:29
type TaskFunc func(ctx context.Context, log logger.Logger)
```

注册：

```go
manager := cronx.NewManager(l)
manager.AddTask("dau_refresh", "5 0 * * *", func(ctx context.Context, log logger.Logger) {
    // do work
})
```

### 2.3 使用方

#### bff

- `bff/ioc/cron.go:14` 定义 `dauCronSpec = "5 0 * * *"`（每天 00:05）。
- `bff/ioc/cron.go:16-25` `InitCronxManager` 创建 manager 并注册 DAU 刷新任务。
- `bff/ioc/cron.go:27-42` `registerDAURefreshTask`：
  - `cron.NewDAURefresher(...)` 构造刷新器；
  - `refresher.Bootstrap(ctx)` 启动时跑一次做兜底恢复；
  - `cronMgr.AddTask("dau_refresh", dauCronSpec, refresher.Refresh)` 注册定时任务。
- DAU 刷新器实现 `bff/cron/dau.go`（116 行）：
  - 用 `redsync` 拿分布式锁 `dau:daily:lock`，TTL 2 分钟，避免多 BFF Pod 重复写 Prometheus Gauge + Redis 兜底 key。
  - 启动时 `Bootstrap` 从 `dau:latest` 恢复昨日的 DAU 值；定时 `Refresh` 把昨日 96 个 15 分钟桶 `PFMERGE` 后写入 Gauge。

#### be-classlist

- `be-classlist/internal/biz/classer.go:75-86` 在 usecase 构造时直接 new manager 并注册：
  - `cache_class_list` `0 5 * * *`（每天 05:00 预热课表缓存）
  - `clean_recycle_bin` `0 2 1 * *`（每月 1 日 02:00 清理回收站 zset）
- 任务实现 `cluc.cacheClass` / `cluc.cleanRecycleBinTask`（同文件 556-647 行），使用 worker pool + 令牌桶限流（`golang.org/x/time/rate`，QPS=200，16 workers）。
- usecase `Close()` 中调用 `cluc.cronManager.Stop(context.Background())` 优雅停机。

### 2.4 典型任务模板（推荐写法）

```go
// 在 ioc 层注册
func InitCronxManager(l logger.Logger, deps ...any) *cronx.Manager {
    mgr := cronx.NewManager(l)
    if err := mgr.AddTask("my_task", "0 3 * * *", func(ctx context.Context, log logger.Logger) {
        if err := doWork(ctx, log); err != nil {
            log.Error("my_task failed", logger.Error(err))
        }
    }); err != nil {
        l.Warn("register my_task failed", logger.Error(err))
    }
    return mgr
}
```

## 3. 主线 A：直接使用 `robfig/cron/v3`

### 3.1 be-class（`internal/timedTask`）

- `be-class/internal/timedTask/timedTask.go` 持有 `*cron.Cron` 字段，启动 `c.Start()`、停机 `c.Stop()`。
- 三个注册入口：
  - `RegisterAddClassInfosToESTask`（`timedTask.go:36-63`）：启动先跑一次（包含 ES 写入 + 空闲教室刷新 + 5s 等待），再注册 `0 3 * * *` 每天 03:00 跑。
  - `RegisterClearClassInfoTask`（`timedTask.go:66-79`）：`0 5 * * *` 每天 05:00 清理 ES 课程信息 + 教室占用。
  - `RegisterCrawFreeClassroomTask`（`timedTask.go:81-115`）：`0 4 * * 1` 每周一 04:00 拉空闲教室，启动时先跑一次。
- `AddTask(spec, fn)`（`timedTask.go:118-125`）包装 `c.AddFunc`，错误直接 `panic`。
- 日志：项目自有 `clog.LogPrinter`（非 `common/pkg/logger`），无 panic recover。

### 3.2 be-proxy（`service/proxy.go`）

- `be-proxy/service/proxy.go:72-79`：
  ```go
  c := cron.New(cron.WithSeconds())
  spec := fmt.Sprintf("@every %ds", s.PollInterval)
  _, err := c.AddFunc(spec, s.fetchIp)
  c.Start()
  ```
- **唯一使用 6 字段（带秒）的位置**。`@every Ns` 周期拉代理 IP。
- 构造时同步 `s.fetchIp()` 跑一次，保证启动即可用。
- 注意：该 `cron.Cron` 没有 `Stop()` 兜底，进程退出靠 OS 信号。

### 3.3 common/bizpkg/proxy（`http.go`）

- `common/bizpkg/proxy/http.go:98-100`：
  ```go
  c := cron.New()
  _, _ = c.AddFunc("@every 15s", globalProxy.update)
  c.Start()
  ```
- 被 `NewHttpProxy(p, l)` 调用，每 15 秒通过 gRPC `be-proxy` 刷新全局代理地址。
- 同样没有 Stop 兜底。

## 4. 伪 cron：ticker 那一支

### 4.1 共同模式

三个服务（`be-content` / `be-elecprice` / `be-feed`）的 `cron` 子包用同一套骨架：

```go
type Controller struct {
    durationTime time.Duration
    stopChan     chan struct{}
    l            logger.Logger
    // ...
}

func (c *Controller) StartCronTask() {
    go func() {
        ticker := time.NewTicker(c.durationTime)
        for {
            select {
            case <-ticker.C:
                // do work
            case <-c.stopChan:
                ticker.Stop()
                return
            }
        }
    }()
}
```

`durationTime` 来自 yaml（如 `cfg.CalendarController.DurationTime * time.Hour`）。

对外暴露 `Cron` 接口（`be-content/cron/cron.go:3-5`）：

```go
type Cron interface {
    StartCronTask()
}
```

`wire.go` 通过 `cron.NewCron(...) []Cron` 聚合，`main.go` 在 `app.Start()` 中遍历启动：

```go
// be-content/main.go:48-50
for _, c := range app.crons {
    c.StartCronTask()
}
```

### 4.2 具体任务

| 服务 | 文件 | 任务 | 周期来源 |
|---|---|---|---|
| `be-content` | `cron/calendar.go:43-59` | 爬华师日历 PDF/图片 → 上传七牛 → 写库 | `cfg.CalendarController.DurationTime` 小时 |
| `be-elecprice` | `cron/elecprice.go:44-65` | 查待推送电费消息 → 调 `be-feed.PublicFeedEvent` | `cfg.ElecpriceController.DurationTime` 小时 |
| `be-feed` | `cron/muxi.go:41-57` | 拉木犀官方待发布消息 → 写入 feed | `cfg.MuxiController.DurationTime` 秒 |
| `be-feed` | `cron/holiday.go` | 节假日事件生成 | 周期同上 |

### 4.3 已知问题

- **名字误导**：`cron` 包没有用 cron 库。
- **粒度受限**：只能等距 `time.Duration`，无法用 5/6 字段表达式；想做"每天 9 点"要自己换算。
- **缺 panic recover**：任务 panic 直接退出 goroutine，调度停摆。
- **缺统一日志**：每个 controller 自己 `r.l.Error(...)`，没有 `task_id/spec` 上下文。
- **关闭不优雅**：`close(stopChan)` 不等 goroutine 真正退出，可能丢尾。
- **没有分布式协调**：多副本同跑，资源浪费；目前任务幂等所以未出问题。

## 5. 选型建议

### 5.1 新增定时任务

- **优先用 `common/pkg/cronx.Manager`**，放在服务的 `ioc/` 层。
- 任务签名遵循 `func(ctx context.Context, log logger.Logger)`。
- 涉及多副本竞争的资源（写外部状态、消耗配额），**必须**用 `redsync` 或 etcd 抢锁。
- 频率 ≤ 1 min 的高频任务（如心跳），考虑直接用 `time.Ticker` 单独 goroutine，不要走 cron。

### 5.2 现有迁移候选

按收益排序：

1. **`be-content` / `be-elecprice` / `be-feed` 的 ticker 模式 → `common/pkg/cronx`**
   - 收益：panic recover、统一日志、`Stop` 优雅停机、配置粒度从"等距小时"升级为"5 字段表达式"。
   - 改动：删除每个 service 内的 `stopChan` + `ticker` 循环，迁移为 `mgr.AddTask("calendar_refresh", "0 9 * * *", c.Run)` 风格。
2. **`be-class` 的 `internal/timedTask` → `common/pkg/cronx`**
   - 收益：统一日志、panic recover、不再依赖项目自有 `clog`。
   - 改动：替换 `cron.Cron` 为 `cronx.Manager`，调整 `AddTask` 签名为 `(id, spec, TaskFunc)`。
3. **`common/bizpkg/proxy.http` + `be-proxy` 的 cron.Stop 兜底**
   - 收益：进程退出时少一些 goroutine 残留。
   - 改动：把 `*cron.Cron` 字段提升到结构体，暴露 `Close(ctx)`，并按 [[cron 关闭兜底]] 接入 Kratos 停机链路。

### 5.3 不建议改的

- **bff 的 DAU 刷新**：已经是 `cronx`，且加了 `redsync` 分布式锁，模式成熟。
- **be-classlist 的 cronx 嵌入方式**：可以挪到 `ioc/` 层以统一形态，但行为正确性不受影响。

## 6. 一些延伸观察

- **没有时区配置**——所有 cron 表达式按容器时区走；BFF/服务时区漂移会导致 `00:05` 任务偏。`cronx` 留了 `WithCronOptions` 扩展口（`cronx/cron.go:72-76`），可注入 `cron.WithLocation(time.Local)` 或固定 `Asia/Shanghai`。
- **没有统一的注册/发现机制**——各服务自己 `wire.NewSet` 自己的 `NewCron(...)`，未来加新定时任务要改多处 wire 文件；可以做一个 `ioc.RegisterCronTasks(mgr, logger)` 模式统一收口。
- **没有任务指标**——`cronx` 输出了 duration 日志但没有 Prometheus 指标；想看任务成功率/耗时分布需要再包一层 middleware（`cronx` 的 `WithCronOptions` 提供了 `cron.Chain` 的接入位）。
- **超时控制**——任务拿到的 `ctx` 是 `cronx` 内部 `baseCtx`（默认 `context.Background()`），没有 deadline；如果任务可能跑很久，**任务内部需要自己 `context.WithTimeout`**。

## 7. 快速索引

| 文件 | 行 | 内容 |
|---|---|---|
| `common/pkg/cronx/cron.go` | 1-230 | `Manager` / `TaskFunc` / 选项 |
| `bff/ioc/cron.go` | 1-42 | `InitCronxManager` + `registerDAURefreshTask` |
| `bff/cron/dau.go` | 1-116 | DAU 刷新器（redsync + PFMERGE） |
| `be-classlist/internal/biz/classer.go` | 75-86 | cronx 嵌入 usecase + 两条任务 |
| `be-class/internal/timedTask/timedTask.go` | 1-137 | robfig/cron + 三类注册方法 |
| `be-proxy/service/proxy.go` | 72-79 | `WithSeconds` + `@every Ns` 拉代理 |
| `common/bizpkg/proxy/http.go` | 98-100 | `@every 15s` 刷新全局代理 |
| `be-content/cron/calendar.go` | 43-59 | ticker 爬日历 |
| `be-elecprice/cron/elecprice.go` | 44-65 | ticker 电费推送 |
| `be-feed/cron/muxi.go` | 41-57 | ticker 木犀消息 |
| `be-feed/cron/holiday.go` | — | ticker 节假日生成 |

cronx 源码
```go
package cronx

// Package cronx 提供一个具备任务增删查能力的定时任务管理器，
// 统一接入项目自定义的 logger，便于在任务执行过程输出一致的结构化日志。

import (
	"context"
	"errors"
	"sync"
	"time"

	"github.com/robfig/cron/v3"

	"github.com/asynccnu/ccnubox-be/common/pkg/logger"
)

var (
	// ErrTaskExists 表示同名任务已经存在。
	ErrTaskExists = errors.New("cronx: task ID already exists")
	// ErrTaskNotFound 表示任务不存在。
	ErrTaskNotFound = errors.New("cronx: task ID not found")
	// ErrInvalidTaskID 表示任务 ID 为空。
	ErrInvalidTaskID = errors.New("cronx: task ID must not be empty")
	// ErrNilTaskFunc 表示任务函数为空。
	ErrNilTaskFunc = errors.New("cronx: task func must not be nil")
)

// TaskFunc 定义任务函数签名，统一透出上下文与 logger。
type TaskFunc func(ctx context.Context, log logger.Logger)

// TaskMeta 用于描述已注册任务的基本信息。
type TaskMeta struct {
	ID   string
	Spec string
	Next time.Time
	Prev time.Time
}

// Manager 负责任务的注册、删除、列出以及生命周期管理。
type Manager struct {
	cron    *cron.Cron
	logger  logger.Logger
	baseCtx context.Context

	mu    sync.RWMutex
	tasks map[string]taskEntry
}

type taskEntry struct {
	entryID cron.EntryID
	spec    string
}

// Option 用于定制 Manager 行为。
type Option func(*config)

type config struct {
	ctx         context.Context
	cronOptions []cron.Option
}

// WithContext 指定任务执行时使用的基础 context。
func WithContext(ctx context.Context) Option {
	return func(c *config) {
		if ctx != nil {
			c.ctx = ctx
		}
	}
}

// WithCronOptions 允许追加 robfig/cron 的可选项，例如时区、拦截器等。
func WithCronOptions(opts ...cron.Option) Option {
	return func(c *config) {
		c.cronOptions = append(c.cronOptions, opts...)
	}
}

// NewManager 创建任务管理器，并立即启动底层调度器。
func NewManager(log logger.Logger, opts ...Option) *Manager {
	if log == nil {
		panic("cronx: logger must not be nil")
	}

	cfg := config{
		ctx:         context.Background(),
		cronOptions: []cron.Option{},
	}

	for _, opt := range opts {
		opt(&cfg)
	}

	if cfg.ctx == nil {
		cfg.ctx = context.Background()
	}

	manager := &Manager{
		cron:    cron.New(cfg.cronOptions...),
		logger:  log.AddCallerSkip(1),
		baseCtx: cfg.ctx,
		tasks:   make(map[string]taskEntry),
	}

	manager.cron.Start()
	manager.logger.Info("cron manager started")

	return manager
}

// Stop 停止调度器，直至所有正在执行的任务完成或 context 超时。
func (m *Manager) Stop(ctx context.Context) error {
	if ctx == nil {
		ctx = context.Background()
	}

	doneCtx := m.cron.Stop()

	select {
	case <-doneCtx.Done():
		m.logger.Info("cron manager stopped")
		return nil
	case <-ctx.Done():
		m.logger.Warn("cron manager stop aborted", logger.String("reason", ctx.Err().Error()))
		return ctx.Err()
	}
}

// AddTask 注册一个新的定时任务。
func (m *Manager) AddTask(id, spec string, job TaskFunc) error {
	if id == "" {
		return ErrInvalidTaskID
	}

	if job == nil {
		return ErrNilTaskFunc
	}

	m.mu.Lock()
	defer m.mu.Unlock()

	if _, exists := m.tasks[id]; exists {
		return ErrTaskExists
	}

	entryID, err := m.cron.AddFunc(spec, m.wrapJob(id, spec, job))
	if err != nil {
		return err
	}

	m.tasks[id] = taskEntry{
		entryID: entryID,
		spec:    spec,
	}

	m.logger.Info("cron task registered", logger.String("task_id", id), logger.String("spec", spec))

	return nil
}

// RemoveTask 删除已注册的任务，返回是否删除成功。
func (m *Manager) RemoveTask(id string) bool {
	m.mu.Lock()
	defer m.mu.Unlock()

	entry, ok := m.tasks[id]
	if !ok {
		return false
	}

	m.cron.Remove(entry.entryID)
	delete(m.tasks, id)

	m.logger.Info("cron task removed", logger.String("task_id", id))

	return true
}

// ListTasks 列出当前所有任务的调度信息。
func (m *Manager) ListTasks() []TaskMeta {
	m.mu.RLock()
	defer m.mu.RUnlock()

	metas := make([]TaskMeta, 0, len(m.tasks))
	for id, entry := range m.tasks {
		cronEntry := m.cron.Entry(entry.entryID)
		metas = append(metas, TaskMeta{
			ID:   id,
			Spec: entry.spec,
			Next: cronEntry.Next,
			Prev: cronEntry.Prev,
		})
	}

	return metas
}

func (m *Manager) wrapJob(id, spec string, job TaskFunc) func() {
	return func() {
		taskLogger := m.logger.With(
			logger.String("task_id", id),
			logger.String("spec", spec),
		)

		ctx := m.baseCtx
		if ctx == nil {
			ctx = context.Background()
		}

		start := time.Now()
		taskLogger.Debug("cron task started")

		defer func() {
			duration := time.Since(start)
			if r := recover(); r != nil {
				taskLogger.Error(
					"cron task panic",
					logger.Any("panic", r),
					logger.String("duration", duration.String()),
				)
				return
			}
			taskLogger.Info(
				"cron task finished",
				logger.String("duration", duration.String()),
			)
		}()

		job(ctx, taskLogger)
	}
}
```