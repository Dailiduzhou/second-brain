---
category: ecommerce
type: idempotency
status: seedling
---
## 为什么放弃DTM
### 一、 为什么在你的场景下要“封杀” DTM？
DTM（或者任何基于 TCC/Saga 的分布式事务框架）是为真正的微服务（跨物理机、跨不同数据库实例）设计的。

1. **资源开销不可接受**
2. **过度设计（伪需求）** 
3. **开发成本极高**

### 二、 正确姿势：River + 事务发件箱 (Transactional Outbox)
基于 River 的方案，本质上是利用 PostgreSQL 的本地事务能力，将“业务数据修改”和“异步任务下发”绑定在同一个原子操作中。这是目前单体/共用 DB 架构下最优雅、最省资源的解法。

#### 核心执行流（以支付成功回调为例）：
当 Payment 模块收到微信支付成功的异步通知时：
**步骤 1：开启大事务 (本地原子性)**
在 Go 代码中开启一个 `sql.Tx`。在这个事务内，你要做两件事：

1. **驱动状态机：** 将订单表的状态从 `待支付` 改为 `已支付`。
2. **投递异步任务：** 向 Riverqueue 的 job 表中插入“发积分”、“发站内信”、“真实扣减库存（或核销预扣库存）”的任务。由于 River 和你的业务表在同一个 PG 里，**只要 tx.Commit() 成功，订单状态一定更新了，且 River 任务一定入库了；如果出错回滚，一切当无事发生。**
**步骤 2：River Worker 异步消费 (最终一致性)**
各个模块（User, Mall）的 Worker 在后台拉取这些任务并执行。

- 发积分 Worker：给用户加积分。
- 发信 Worker：生成站内信。

#### 关键防线：Worker 必须做“幂等”处理
由于 River 保证的是至少一次（At-Least-Once）送达，如果 Worker 执行到一半崩溃，或者网络超时，任务会被重试。因此，你的 Worker 逻辑必须是幂等的。

### 三、 具体的代码落地演示
下面是基于你架构的具体伪代码实现：

```go
// 1. 在支付回调或者订单模块的 Biz 层
func (uc *OrderUsecase) HandlePaymentSuccess(ctx context.Context, orderID string, payAmount int) error {
    // 开启本地数据库事务
    tx, err := uc.db.BeginTx(ctx, nil)
    if err != nil {
        return err
    }
    defer tx.Rollback() // 如果后续没执行 tx.Commit()，就会回滚

    // 1. 状态机流转：更新订单状态 (带上乐观锁或原状态校验)
    // UPDATE orders SET status = 'PAID' WHERE id = ? AND status = 'UNPAID'
    rowsAffected, err := uc.orderRepo.UpdateStatusToPaidTx(ctx, tx, orderID)
    if err != nil {
        return err
    }
    if rowsAffected == 0 {
        // 状态不对，可能已经处理过了，直接返回，避免重复发积分
        return nil 
    }

    // 2. 投递后续异步任务到 River (挂载在同一个 tx 上)
    
    // 任务A：发放积分
    _, err = uc.riverClient.InsertTx(ctx, tx, job.AddPointsArgs{
        OrderID: orderID,
        UserID:  123,
        Points:  payAmount / 100, // 假设1元1积分
    }, nil)
    if err != nil { return err }

    // 任务B：发送站内信
    _, err = uc.riverClient.InsertTx(ctx, tx, job.SendMsgArgs{
        UserID:  123,
        Title:   "支付成功",
        Content: "您的订单已支付成功",
    }, nil)
    if err != nil { return err }

    // 3. 提交事务，达成绝对的原子性！
    return tx.Commit()
}

```
接下来是处理积分的 Worker：

```go
// 2. 积分 Worker 的实现 (必须幂等)
type AddPointsWorker struct {
    userRepo biz.UserRepo
}

func (w *AddPointsWorker) Work(ctx context.Context, job *river.Job[job.AddPointsArgs]) error {
    // 【关键】：检查这笔订单的积分是否已经发过了 (幂等性校验)
    // 比如在 user_points_log 表里查一下有没有 order_id = job.Args.OrderID 的记录
    isAdded, err := w.userRepo.CheckPointsAdded(ctx, job.Args.OrderID)
    if err != nil {
        return err // 返回错误，River 会在稍后重试
    }
    if isAdded {
        return nil // 已经发过，直接返回成功，丢弃任务
    }

    // 执行加积分逻辑
    return w.userRepo.AddPoints(ctx, job.Args.UserID, job.Args.Points, job.Args.OrderID)
}

```
### 四、 架构上的一个小提醒：库存扣减的时机
你在问题中提到了“库存扣减”。在电商系统中，库存通常建议在“下单时（创建订单）”**进行**预扣减（利用 Redis 的 Lua 脚本或者 PG 的行锁），而不是等到支付成功才扣减。
如果支付成功才扣减，极易出现“超卖”问题（用户付了钱，但商品没货了，此时只能走繁琐的退款流程）。而在支付成功后，你通过 River 异步做的通常是“扣除真实库存 / 擦除预扣库存”的操作。

## 库存预扣减
**最佳方案：PostgreSQL 乐观锁预扣减 + River 延迟任务释放库存。**

#### 1. 核心流程
1. 用户点击下单。
2. 开启一个 PG 本地事务 (`tx`)。
3. 利用 SQL 原生的原子性，直接在数据库层尝试扣减库存：`UPDATE sku SET stock = stock - N WHERE id = ? AND stock >= N`。
4. 如果扣减成功（受影响行数为 1），生成订单，并**向 River 插入一个“15分钟后执行”的订单超时检查任务**。
5. 如果扣减失败（受影响行数为 0），说明库存不足，回滚事务，直接返回给前端。

## 死信队列(DLQ)
你可以直接在 Kratos 中启动一个全局的 Cron 任务，或者将它包装成一个简单的 River 定时任务（Periodic Job），每隔 5 分钟去 `river_job` 表里看一眼。

在 Grafana 中配置一个 SQL 数据源（直接连你的 PostgreSQL），写一句查询： `SELECT count(*) FROM river_job WHERE state = 'discarded'` 然后在 Grafana 里配置一条 Alert 规则：“当这个查询结果 > 0 时，通过 Webhook 发送飞书通知”。