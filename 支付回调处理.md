---
category: ecommerce
type: integration
topic: payment
frameworks:
  - go-kratos
  - gopay
module: payment
status: seedling
aliases:
  - 支付异步通知处理
tags:
  - ecommerce/payment
  - microservice/go-kratos
  - reliability/idempotency
---

# 支付回调处理

支付回调属于第三方异步通知，不走普通 Protobuf JSON API。做法是：Raw Route 接入原始 `*http.Request`，先验签，再交给 Biz 层做幂等状态流转，最后返回渠道要求的固定响应。

## 处理链路

```text
HTTP Raw Route -> Service -> Biz -> Repo/Adapter -> DB
```

- Route 只接收原始请求。
- Service 只做转发。
- Biz 负责验签后的状态更新和副作用编排。
- Repo/Adapter 负责第三方协议细节。

## 路由入口

```go
func NewHTTPServer(c *conf.Server, paymentSvc *service.PaymentService) *http.Server {
    srv := http.NewServer(/* opts */)
    v1.RegisterPaymentHTTPServer(srv, paymentSvc)

    srv.HandleFunc("/v1/payment/alipay/notify", func(w http.ResponseWriter, r *http.Request) {
        if err := paymentSvc.HandleAlipayNotify(r.Context(), r); err != nil {
            w.WriteHeader(http.StatusInternalServerError)
            _, _ = w.Write([]byte("fail"))
            return
        }
        _, _ = w.Write([]byte("success"))
    })

    return srv
}
```

支付宝异步通知成功时要返回严格的 `success`，失败返回 `fail` 或非 2xx。

## Service 层

```go
func (s *PaymentService) HandleAlipayNotify(ctx context.Context, r *http.Request) error {
    return s.paymentUsecase.HandleNotify(ctx, biz.ChannelAlipay, r)
}
```

Service 不直接验签和更新数据库。

## Biz 层

```go
func (uc *PaymentUsecase) HandleNotify(ctx context.Context, channel PayChannel, r *http.Request) error {
    return uc.tx.InTx(ctx, func(ctx context.Context) error {
        notify, err := uc.repo.VerifyNotify(ctx, channel, r)
        if err != nil {
            return err
        }
        return uc.applyPaidResult(ctx, notify)
    })
}
```

`applyPaidResult` 必须幂等，例如：

```sql
-- name: MarkPaymentSuccess :one
UPDATE payments
SET status = 'paid',
    third_party_tx_id = $2,
    paid_at = now()
WHERE order_no = $1
  AND status = 'pending'
RETURNING id, order_id;
```

没有返回行时，说明支付单已处理、状态不允许流转或订单不存在，不能重复触发发货等副作用。

## gopay 验签

支付宝异步通知通常是 `application/x-www-form-urlencoded`，先 `ParseForm()` 再验签。`ParseNotifyToBodyMap` 接收的就是标准 `*http.Request`。

```go
func (a *AlipayAdapter) VerifyNotify(r *http.Request) (*biz.PaymentNotify, error) {
    bm, err := alipay.ParseNotifyToBodyMap(r)
    if err != nil {
        return nil, err
    }

    ok, err := alipay.VerifySign(a.publicKey, bm)
    if err != nil || !ok {
        return nil, errors.New("invalid notify signature")
    }

    return &biz.PaymentNotify{
        Channel:        biz.ChannelAlipay,
        OrderNo:        bm.GetString("out_trade_no"),
        ThirdPartyTxID: bm.GetString("trade_no"),
        TradeStatus:    bm.GetString("trade_status"),
        Amount:         bm.GetString("total_amount"),
    }, nil
}
```

证书模式可改用：

```go
ok, err := alipay.VerifySignWithCert("/path/to/alipayPublicCert.crt", bm)
```

## 关键注意点

- 先验签，再相信金额、订单号和交易号。
- 回调和 River 查单可能同时成功，数据库更新必须带原状态条件。
- 回调接口不要依赖用户 JWT。
- 返回值必须匹配渠道要求，支付宝只认 `success` / `fail`。
- 支付成功后的副作用放到 [[支付查单与 River 兜底]] 的同一套状态机里处理。

## 相关链接

- [[Go-Kratos-支付系统集成指南]]
- [[Alipay gopay 适配器]]
- [[支付查单与 River 兜底]]
- [[PaymentSync Repo 事务改造]]
