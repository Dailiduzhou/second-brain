---
category: ccnubox
type: hub
status: seedling
tags:
  - ccnubox
  - ccnubox/hub
---

# 华师匣子

[华师匣子](https://ccnubox.muxixyz.com/)是一个基于 Go 的 12 服务微服务架构。

## 概览

- **服务规模**：12 个微服务 + 1 个 BFF + 1 个 `common` 共享模块
- **构建方式**：Go 1.26+ workspace（`go.work`）
- **通信**：BFF 对外 HTTP/8080，服务间 gRPC
- **服务治理**：etcd 注册中心
- **异步**：Kafka 事件
- **存储**：MySQL + Redis + Elasticsearch
- **追踪**：OpenTelemetry

## 笔记导航

> 按照"先框架、再实现、后问题"的层次组织。同类笔记聚合在一起，便于按主题阅读。

### 架构与基础

| 笔记 | 描述 |
|------|------|
| [[华师匣子框架]] | 项目技术栈总览：Google Wire DI、手写 Kratos 风格 gRPC、Gin BFF、etcd、Kafka、MySQL/Redis/ES 选型与分层架构。 |
| [[华师匣子-代码结构]] | 仓库目录树、Go workspace 布局、`make` 构建脚本与服务启动约定。 |
| [[华师匣子BFF结构]] | BFF 层的目录划分、路由注册、接口编排与对 App 端的网关职责。 |
| [[整体gRPC 配置字段]] | `common/bizpkg/conf/type.go` 中 `GrpcConf` 的字段语义、server/client 拆分方案与 EtcdTTL 单位统一。 |
| [[gRPCx的封装]] | `common/pkg/grpcx` 的源码解析：Kratos gRPC Server 注册、etcd lease/keepalive、SWRR 负载均衡实现与潜在问题清单。 |
| [[go-kratos 中间件实现]] | Kratos 中间件本质（`func(handler) handler`）与在 BFF 鉴权 / 日志 / 链路中的典型写法。 |

### 通用能力

| 笔记                   | 描述                                                                                                |
| -------------------- | ------------------------------------------------------------------------------------------------- |
| [[整体代理(proxy)使用]]    | `common/bizpkg/proxy` 的设计：`HttpProxy` / `HttpTransport` / `HttpClient` 三层封装、函数选项式 API、开发与生产环境代理切换。 |
| [[Failover 代理传输层实现]] | `FailoverTransport` 的实现：Pin 机制、Failover、跨代理负载均衡、Transport 缓存、POST Body 重放与已知缺陷。            |
| [[定时任务实现]]           | `robfig/cron/v3` 的两种封装方式、任务注册表、BFF 与微服务中的典型定时任务。                                          |
| [[cron 关闭兜底]]        | 进程退出时 `cron.Cron` 引用丢失导致的优雅停机缺口，对接 Kratos `app.Stop` 的修复方案。                              |
| [[robfig cron 的使用]]  | `robfig/cron/v3` 的 cron 表达式、parser、Location、自定义 Job 类型等基础用法。                            |
| [[华师匣子限流中间件]]        | `common/pkg/limiter` 抽象接口、Redis 滑动窗口 + Lua 限流、潜在问题与调用方现状。                                  |
| [[华师匣子日志封装]]         | `common/pkg/logger` 装饰器 + 适配器分层：zap 后端、敏感脱敏、OTel trace 注入、Kratos / GORM 适配器及潜在问题。       |
| [[华师匣子用户活跃度统计]]      | `common/pkg/tieredx` 源码解析：按活跃度分 3 档的定时刷新调度，ants 协程池 + 令牌桶双层并发控制，redsync 多实例互斥。            |
| [[华师匣子crawler 优化]]   | `be-ccnu` 4 个 crawler 在 `*http.Client` 管理上的不一致问题与 `crawlerx.Session` 抽象改造方案。             |

### BFF 鉴权与中间件

| 笔记 | 描述 |
|------|------|
| [[BFF 中间件错误处理]] | Gin 中间件三步退出法（写响应 / `Abort`）与本项目自定义 `errorhandling` 中间件的实现差异。 |
| [[BFF JWT部分耦合过重]] | JWT/Session 模块的六大耦合问题：`ClearToken` 职责混合、`CheckSession` 语义反转、密码嵌入 JWT、缺失滑动过期等。 |
| [[BFF JWT 中存储加密密码的安全性分析]] | 把加密后的密码写入 JWT Claims 的全链路风险分析（解密成本、Token 体积、轮换困难）。 |
| [[BFF AES加密优化]] | 每次请求 `cipher.NewGCM` 的性能损耗、进程级 `AEAD` 缓存方案与并发安全验证。 |
| [[前端登录持久化方案]] | 前端持久化机制、端到端认证体系的问题诊断与重构建议（与 BFF JWT 方案配套）。 |

### BFF gRPC / 数据 / 可观测性

| 笔记 | 描述 |
|------|------|
| [[BFF 取gRPC结构体字段风险报告]] | 直接 `gRPCResp.Field` 取值的 nil pointer 风险，区分嵌套 message、切片元素等场景并给出安全写法。 |
| [[BFF 中表示不可用的接口列表]] | library / BFF 中被刻意留作 "占位但未实现" 的接口清单，附对调用方的影响。 |
| [[BFF DAU实现]] | 基于 Redis HyperLogLog + Gauge + cron 的 DAU 统计：写入、查询、容灾与一致性策略。 |
| [[BFF DAU验证与运维]] | DAU 的 Redis key 设计、单元测试覆盖、监控指标与排障 checklist。 |
| [[BFF Otel使用]] | BFF / 后端 / Kafka / 日志链路的 OpenTelemetry 接入全流程、传播模型与实现细节。 |
| [[BFF OTel实践与排障]] | 团队内的 OTel 实践约定（哪些 span 必打、propagation 边界）与常见排障路径。 |
| [[BFF logger中间件的问题]] | `errors.Unwrap` 引起的空指针、双重响应、敏感 Header 全量打印等 logger 中间件隐患。 |

### 重点学习

[[华师匣子课表查询]]

### 学习中

- [[Basic TODO List]] 团队项目区：
  - 华师匣子BFF 阅读
  - 华师匣子配置读取部分

## 服务一览

| 服务 | 端口 | 职责 |
|------|------|------|
| `bff` | 8080 | BFF 聚合层，对接前端 |
| `be-content` | 19081 | 校历、部门、信息汇总、banner |
| `be-ccnu` | 19082 | 一站式登录 |
| `be-class` | 18000 / 19083 | 蹭课、空闲教室 |
| `be-classlist` | 19084 | 课表管理（v1） |
| `be-classlist_v2` | — | 课表管理（v2，重写版） |
| `be-counter` | 19085 | 核心用户判断 |
| `be-elecprice` | 19087 | 电费查询 |
| `be-feed` | 19088 | 消息推送 |
| `be-grade` | 19089 | 成绩查询 |
| `be-user` | 19091 | 用户服务、cookie 管理 |
| `be-library` | 19093 | 图书馆 |
| `be-proxy` | 19094 | IP 代理池 |

完整分层与服务依赖图见 [[华师匣子框架#分层架构]]。

## 跨主题链接

- [[微服务]] — 项目级微服务总览
- [[go-kratos架构]] — 借鉴的 Kratos 分层风格
- [[kratos layout]] — 各服务 layout 模板
