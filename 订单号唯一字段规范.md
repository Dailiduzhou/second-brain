---
category: ecommerce
type: standard
topic: order-no
module: foundation
status: done
tags:
  - ecommerce/foundation
  - id/order-no
  - payment
aliases:
  - 唯一字段规范
  - out_trade_no 规范
---

# 订单号唯一字段规范

订单号（`out_trade_no`）是业务系统与第三方支付平台之间的唯一关联字段。不同渠道对长度有硬性约束，本规范统一项目内的生成与使用方式。

## 渠道长度差异

| 渠道 | 字段 | 类型限制 | 项目内规则 |
|------|------|----------|------------|
| 微信支付 | `out_trade_no` | `String(32)` | 使用 [[雪花 ID 生成器]] 的 `GenerateOrderNo32` |
| 支付宝 | `out_trade_no` | `String(64)` | 使用 [[雪花 ID 生成器]] 的 `GenerateOrderNo64` |
| 内部业务 | `order_no` | 不超过 64 | 32 位 / 64 位二选一，与外部渠道对齐 |

> [!warning] 强制要求
> 雪花 ID 和时间戳是保证唯一性的根本，**绝对不能截断**；如果超长，只能截断“业务前缀”。

## 为什么严格控制位数

- **第三方兼容**：支付宝对 `out_trade_no` 限制最大 64 位，微信限制 32 位。
- **数据库索引对齐**：固定长度字符串在 B-Tree 中比较代价更稳定，避免索引膨胀。
- **分库分表路由**：固定长度是 `HASH(order_no) % N` 等 Sharding Key 的前提。
- **日志排查规范化**：固定格式便于在 ELK / Loki 中按正则聚合。

## 32 位订单号格式

`业务前缀(2位) + 时间戳(14位) + 雪花ID十六进制(16位) = 32位`

> [!example] 示例
> `PA202601141530091a3f8c2e7b0d4f6a8`
> - `PA` = 支付业务前缀
> - `20260114153009` = 2026-01-14 15:30:09
> - `1a3f8c2e7b0d4f6a8` = 雪花 ID 十六进制

## 64 位订单号格式

`业务前缀(4位) + 时间戳(14位) + 用户ID补齐(8位) + 雪花ID补齐(19位) + 随机串(19位) = 64位`

> [!example] 示例
> `PAY_2026011415300900000123123456789012345678ABCDEFGHIJKLMNOPQRS`
> - `PAY_` = 业务前缀
> - `20260114153009` = 时间戳
> - `00000123` = 用户 ID
> - 雪花 ID 19 位十进制
> - 19 位密码学安全随机串

## 业务前缀约定

| 业务 | 前缀 |
|------|------|
| 支付订单 | `PA` / `PAY_` |
| 退款单 | `RF` / `REF_` |
| 订单主单 | `OD` / `ORDR` |
| 内部业务流水 | `IN` / `INNT` |

前缀在 `biz` 层做字典校验，避免脏数据进入下游。

## 业务使用流程

1. **下单**：由 [[IDGenerator 抽象接口]] 在 `data` 层生成，落到 `orders` / `payments` 表唯一索引列。
2. **创建支付**：`out_trade_no` 直接复用订单号或前缀变体（见 [[Alipay gopay 适配器]]、[[Wechat Pay gopay 适配器]]）。
3. **回调查单**：用 `out_trade_no` 反查本地支付单，更新状态时必须幂等（见 [[支付回调处理]]）。
4. **查单兜底**：River 任务以 `order_no` 为关联键主动查第三方（见 [[支付查单与 River 兜底]]）。

## 相关链接

- [[雪花 ID 生成器]]
- [[IDGenerator 抽象接口]]
- [[Alipay gopay 适配器]]
- [[Wechat Pay gopay 适配器]]
- [[支付回调处理]]
