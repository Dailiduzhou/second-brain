# 笔记整理总则

本仓库是 Obsidian 知识库。Agent 的目标是把材料整理成可检索、可复用、可追溯的技术笔记，而不是单纯润色原文。

> [!important] 修改边界
> 只修改用户指定或完成任务必需的笔记。保留原文中的事实、结论和不确定性；没有证据时不要补写成确定结论。不要顺手批量重排、重命名或格式化无关文件。

## 开始前

1. 阅读目标笔记、它直接链接的上级入口笔记，以及引用它的相关笔记。
2. 判断任务属于补充、改写、拆分、合并还是链接修复；不为“看起来更整齐”而拆分。
3. 查看相邻笔记的 frontmatter 和排版，沿用仓库现有格式。元数据字段遵循仓库的 [[Metadata 分类规范]]。
4. 涉及 `source/` 中的代码时，先阅读 [[Git submodule 使用规范]]，确认源码路径和分析所依据的 commit。
5. 修改前运行 `git status --short`，识别并保留用户已有改动。

## 原子笔记

一篇笔记应围绕一个可以独立回答的问题、概念或结论展开，并且脱离原始长文后仍然有复用价值。

适合拆分的信号：

- 同一篇文章回答了多个可独立检索的问题。
- 某一章节可以被多篇笔记复用或引用。
- 背景、实现、排障、验证各自已经形成完整主题。
- 大量二级标题之间只有项目归属关系，没有紧密的论证依赖。

不应拆分的情况：

- 内容很短，拆分后只剩定义或零散清单。
- 多个章节共同支撑同一个结论，单独阅读会丢失前提。
- 拆分只会制造导航负担，没有提升检索或复用价值。

拆分时：

1. 原笔记保留为概览或 hub，写清主题边界并链接子笔记。
2. 子笔记在开头用自然语言链接回概览；概览也必须链接到子笔记。
3. 把公共背景放在概览，子笔记只保留理解当前主题所需的最小上下文。
4. 不机械复制段落；重复内容应收敛到一个权威笔记，由其他笔记引用。

## Obsidian 格式

- 仓库内笔记使用 `[[笔记名]]`、`[[笔记名#标题]]` 或 `[[笔记名|显示文本]]`。
- 外部网页使用标准 Markdown 链接；源码文件按 [[Git submodule 使用规范]] 使用仓库相对路径。
- 新建、拆分或重命名笔记后，检查出链和入链。除 `AGENTS.md`、`README.md` 外，相关文章应建立有语义的双向链接。
- 新笔记必须能从 [[Index Page]]、领域 hub 或其父笔记到达，禁止产生孤儿笔记。
- 链接前确认目标文件和标题真实存在，不创建“以后也许会写”的空 wikilink。
- 不为增加图谱连线而堆砌“相关链接”；正文中的链接应说明二者关系。
- 重要结论使用 `> [!important]`，风险使用 `> [!warning]`，背景或版本信息使用 `> [!note]`。Callout 应有明确标题和简短正文，不把整篇文章塞进引用块。
- 沿用项目已有 formatter 或相邻笔记格式。若仓库没有可执行的 formatter 配置，不要自行引入工具或对全库做机械格式化。

## Frontmatter

- 保留已有且仍然正确的 properties，不随意更换字段名或值类型。
- 新笔记至少包含与同类笔记一致的 `category`、`type`、`topic`、`status` 和 `tags`。
- 多值字段使用 YAML 列表；分类和标签使用小写 kebab-case，层级标签使用 `/`。
- 只有确有检索价值时才添加 `aliases`，不要把标题的大小写或标点变体全部列为别名。

## 技术文风

- 先写结论或用途，再写原理、证据、步骤和限制。
- 使用技术文档语言，术语保持一致；可以吐槽，但不能遮蔽事实或降低可读性。
- 区分事实、推断和建议。对尚未验证的内容明确标注“待验证”及验证方法。
- 标题应表达问题或主题，避免“其他”“补充”“一些想法”这类无检索价值的命名。
- 代码块只保留支撑论点的最小片段；省略内容用注释说明，不得省略会改变语义的错误处理、并发或事务边界。
- 优先解释代码承担的职责、调用关系和设计取舍，不逐行翻译源码。

## Git submodule 与源码

`source/` 用于长期追踪和引用外部代码，默认视为只读材料。

> [!warning] 禁止隐式改动源码
> 查找路径、符号、调用关系或版本信息不构成修改授权。未经用户明确要求，不得更新 submodule、切换 commit、改变 gitlink、编辑 `source/` 或清理其中的已有改动。

- 在 `source/` 内定位或理解代码时，先检查目标源码仓库根目录是否存在 `.codegraph/`。
- 存在 CodeGraph 索引时，优先使用 CodeGraph MCP 的 `codegraph_explore`，并把目标源码仓库作为 `projectPath`；适合查询符号实现、调用路径和影响范围。
- 没有 `.codegraph/` 或 CodeGraph 不可用时，使用 `rg`、`rg --files` 在目标源码仓库内搜索。不要自行创建或更新 CodeGraph 索引。
- CodeGraph MCP 和 ripgrep 都属于只读检索手段；使用它们不允许顺带格式化、修改或暂存源码。
- 引用源码的笔记应在正文开头用 `> [!note]` 标明 submodule 路径和确切 commit；不要使用“最新代码”“当前版本”等不可复现表述。
- 如果 submodule 的检出版本与父仓库 gitlink 不一致，或工作区已有修改，停止会覆盖内容的操作，报告状态并等待用户决定。
- 不得把 `source/` 内的源码作为普通文件加入父仓库，也不得为了整理笔记而提交上游代码改动。

## 完成检查

提交结果前至少确认：

- frontmatter 与同类笔记一致，正文结构符合原子笔记原则。
- 新增 wikilink 的目标存在，章节链接的标题准确，拆分后的父子笔记互相可达。
- 新笔记不是孤儿笔记；已有入口笔记已按需更新。
- 代码路径存在，代码结论与标注的 commit 一致，没有粘贴无关的大块源码。
- `git diff --check` 无空白错误，`git diff -- <涉及文件>` 只包含本次任务需要的修改。
- `source/` 和 submodule 指针没有因整理笔记而改变。

最终回复应列出修改、拆分或新建的笔记，并说明链接关系、验证结果以及任何未解决的不确定性。
