---
category: ecommerce
type: architecture
status: seedling
tags: ecommerce
---
## 模块
### 模组
1. user(区分用户、管理员和商户) 做好RBAC
2. order
3. payment
4. mall(商品，banner，分类，首页推荐，限时活动等)


## layout

单体化应用，不同微服务在**同一进程**中互相调用。

```
mall/
├── api/
│   ├── user/v1/     # 用户接口定义
│   ├── mall/v1/     # 商城接口定义
│   ├── order/v1/    # 订单接口定义
│   └── pay/v1/      # 支付接口定义
├── app/mall-mono/
│   ├── cmd/server/
│   │   ├── main.go  # 启动入口
│   │   └── wire.go  # 依赖注入
│   ├── configs/
│   └── internal/
│       ├── conf/
│       ├── data/
│       │   ├── data.go       # 【全局共享】初始化 DB, Redis, 以及 River Client
│       │   ├── user_repo.go  # 只查写 User 相关表
│       │   ├── mall_repo.go  # 只查写 商品/分类 表
│       │   └── order_repo.go # 只查写 订单 表
│       ├── biz/              # 纯粹的领域逻辑，模块间在此层互相调用
│       │   ├── user.go
│       │   ├── mall.go
│       │   ├── order.go
│       │   └── pay.go
│       ├── service/          # DTO 转换，对接外网 API
│       │   ├── user_svc.go
│       │   └── ...
│       ├── job/              # 【新增】定义 Riverqueue 的具体任务 (Job)
│       │   ├── sync_pay_status.go # 定时去微信/支付宝查单的任务
│       │   └── order_paid.go      # 支付成功后处理订单状态的任务
│       └── server/
│           ├── http.go       # 挂载 user, mall, order, pay 的 HTTP 路由
│           └── worker.go     # 【核心】Riverqueue Worker Server 封装
```

在`biz`层注入`XxxUsecase`来实现互相调用。
```go
// internal/biz/order.go
package biz

import "context"

// OrderUsecase 内部注入了 UserUsecase
type OrderUsecase struct {
    repo     OrderRepo
    userUc   *UserUsecase // 直接调用同一进程内的内存函数
}

func (uc *OrderUsecase) CreateOrder(ctx context.Context, userID int64, goodsID int64) error {
    // 【直接内存调用】，没有 gRPC 序列化，没有网络延迟，CPU 消耗为零
    userInfo, err := uc.userUc.GetUserInfo(ctx, userID)
    if err != nil {
        return err
    }
    
    // 继续创建订单逻辑...
    return nil
}
```


`server` 
```go
// internal/server/http.go
package server

import (
	userV1 "mall/api/user/v1"
	orderV1 "mall/api/order/v1"
	"mall/app/mall/internal/service"

	"github.com/go-kratos/kratos/v2/middleware/recovery"
	"github.com/go-kratos/kratos/v2/transport/http"
)

// 将所有的 Service 实例注入进来
func NewHTTPServer(
	c *conf.Server,
	userSvc *service.UserService,
	orderSvc *service.OrderService,
	logger log.Logger,
) *http.Server {
	var opts = []http.ServerOption{
		http.Middleware(
			recovery.Recovery(),
		),
	}
	// ... 省略网络配置参数读取 ...
    
	srv := http.NewServer(opts...)
    
	// 【核心逻辑】：在同一个路由树下注册所有的微服务 API
	userV1.RegisterUserHTTPServer(srv, userSvc)
	orderV1.RegisterOrderHTTPServer(srv, orderSvc)
    
	return srv
}
```

River transport server
```go
package server

import (
    "context"
    "github.com/riverqueue/river"
    "github.com/go-kratos/kratos/v2/transport"
)

// RiverWorker 封装 River 为 Kratos 的 Server
type RiverWorker struct {
    worker *river.Worker
}

func NewRiverWorker(worker *river.Worker) *RiverWorker {
    return &RiverWorker{worker: worker}
}

// Start 随 Kratos 启动
func (s *RiverWorker) Start(ctx context.Context) error {
    return s.worker.Start(ctx)
}

// Stop 随 Kratos 优雅停机
func (s *RiverWorker) Stop(ctx context.Context) error {
    return s.worker.Stop(ctx)
}

// 确保 RiverWorker 实现了 transport.Server 接口
var _ transport.Server = (*RiverWorker)(nil)
```