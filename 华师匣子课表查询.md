---
title: 华师匣子课表查询服务深度解析
category: ccnubox
type: deep-dive
topic: classlist
status: seedling
tags:
  - ccnubox
  - database
  - database/orm/gorm
  - database/redis
  - database/singleflight
  - kafka
aliases:
  - be-classlist_v2
---

> [!note] 分析版本
> 基于 [commit afcc2ad](https://github.com/asynccnu/ccnubox-be/commit/afcc2adf67ed4056a75357b7e05eba4ffcbf6f59) 进行分析。

## 阅读路线与边界

本文描述 `be-classlist_v2` 的静态结构、数据访问方式和通用工具逻辑，适合作为理解仓库的入口。用户一次“查询课表”请求如何在本地数据、爬虫和延迟重试之间选择，单独展开在 [[华师匣子课表查询-GetClass调用链]]；Kafka 消费者组为何能在重平衡后持续消费，见 [[Sarama ConsumerGroup 使用解析]]。

文中的“本地数据”指 Redis 缓存与 MySQL 持久化数据，而不是客户端本地缓存。服务把教务系统爬虫当作较慢、可能失败的外部数据源：正常读路径应尽量命中本地，只有在本地没有可用结果或调用者显式要求刷新时才触发外部访问。这个边界决定了后文的缓存、事务和降级策略。

## 源码索引

源码通过 git submodule 固定在 `source/ccnubox-be`，本文分析对应提交为 `afcc2ad`。下面的链接均指向仓库中的 `be-classlist_v2` 实际源码：

| 主题 | 源码引用 |
|---|---|
| gRPC 入口 | [grpc/classlist.go](source/ccnubox-be/be-classlist_v2/grpc/classlist.go) |
| Service 参数处理 | [service/classlist.go](source/ccnubox-be/be-classlist_v2/service/classlist.go) |
| 业务主流程 | [biz/usecase/classer.go](source/ccnubox-be/be-classlist_v2/biz/usecase/classer.go) |
| 爬虫、singleflight 与合并 | [biz/usecase/classer_helpers.go](source/ccnubox-be/be-classlist_v2/biz/usecase/classer_helpers.go) |
| Redis 缓存 | [repository/cache/student_course.go](source/ccnubox-be/be-classlist_v2/repository/cache/student_course.go) |
| 事务上下文 | [pkg/transaction/transaction.go](source/ccnubox-be/be-classlist_v2/pkg/transaction/transaction.go) |
| 周数解析与学期校验 | [pkg/tool/tool.go](source/ccnubox-be/be-classlist_v2/pkg/tool/tool.go) |

课表查询从 gRPC 到爬虫、Kafka 重试的完整调用链，见 [[华师匣子课表查询-GetClass调用链]]；该文也按调用顺序列出对应源码文件。

## 核心设计目标

- **读多写少的课表访问**：通过 Cache-Aside 减少数据库与教务系统的压力。
- **刷新不阻塞可用性**：刷新失败或等待超时时，优先返回上一次可用的本地结果。
- **多表写入保持一致**：课程实体与学生选课关系必须作为一个事务提交或回滚。
- **用户编辑不被刷新覆盖**：爬虫写入的官方课与用户添加、备注的自定义信息需要区分和合并。
- **外部失败可恢复**：爬虫失败转为异步延迟重试，而不是在同步 RPC 内无限重试。

## 项目结构

`be-classlist_v2` 的整体架构遵循 DDD 分层：gRPC 层只做协议适配，`service` 层负责参数校验与默认值填充，`biz` 层持有核心业务逻辑，`repository` 层封装所有数据访问。

```
依赖方向：gRPC → service → biz → repository / crawler
```

目录结构如下：

```
.
├── biz/
│   ├── biz.go               # 接口定义（Repo、Service 接口）
│   ├── errors.go
│   ├── model/classer.go     # 业务对象（BO）
│   └── usecase/
│       ├── classer.go       # GetClasses 主流程
│       ├── classer_helpers.go  # 爬虫、singleflight 等工具函数
│       └── usecase.go
├── client/                  # 外部 HTTP 客户端（CCNU 教务系统）
├── crawler/                 # 爬虫实现（三种策略）
├── events/                  # Kafka 延迟队列
│   ├── consumer/
│   ├── delay/delay.go       # DelayKafka 封装
│   ├── producer/
│   └── topic/topic.go
├── grpc/
│   ├── classlist.go         # gRPC handler
│   └── mapper.go            # model → proto 转换
├── pkg/
│   ├── tool/tool.go         # ParseWeeks / FormatWeeks / CheckSY
│   └── transaction/transaction.go  # context 传递事务
├── repository/
│   ├── cache/               # Redis 缓存层
│   ├── dao/                 # GORM DAO 层
│   ├── model/               # DB 数据对象（DO）
│   ├── class.go             # ClassRepo 聚合
│   └── mapper.go            # DO ↔ BO 转换
└── service/
    ├── classlist.go         # Service 层：参数校验与默认值
    └── errors.go
```

## API 契约（Protobuf）

`Classer` 服务对外暴露以下 RPC 接口：

```protobuf
service Classer {
    rpc GetClass(GetClassRequest) returns (GetClassResponse);// 获取课表（核心接口）
    rpc AddClass(AddClassRequest) returns (AddClassResponse);  // 添加自定义课程
    rpc DeleteClass(DeleteClassRequest)   returns (DeleteClassResponse);
    rpc UpdateClass(UpdateClassRequest)   returns (UpdateClassResponse);
    rpc GetRecycleBinClassInfos(...)      returns (...);   // 回收站（保留 2 个月）
    rpc RecoverClass(...)                 returns (...);
    rpc GetAllClassInfo(...)              returns (...);   // 供其他服务调用
    rpc GetStuIdByJxbId(...)             returns (...);    // 获取教学班学生 ID
    rpc GetSchoolDay(GetSchoolDayReq)     returns (GetSchoolDayResp);
    rpc UpdateClassNote(...)             returns (...);
    rpc DeleteClassNote(...)             returns (...);
    rpc GetClassNatures(...)             returns (...);  // 获取课程性质集合
}
```

> [!tip] 关注点
> 核心接口是 `GetClass`，其余大多是 CRUD 操作。`GetClass` 背后藏着完整的缓存-数据库-爬虫三级数据源决策，见 [[华师匣子课表查询-GetClass调用链]]。

从调用方视角看，`refresh` 是一次“尽力刷新”的请求，而不是“必须拿到爬虫最新结果”的强一致承诺。即使刷新出错，服务仍可能返回旧的本地课表；调用方应结合响应中的最后刷新时间判断数据的新鲜度，而不应仅凭 RPC 成功就认定数据已更新。

## Service 层：参数处理

`service/classlist.go` 是 gRPC 与 biz 层之间的适配层，主要职责：填充默认值（year/semester 为空时使用当前学期），并调用下方两个工具函数做校验。

将默认值和输入校验收敛在这一层有两个收益：下层 usecase 不必反复处理空学年、空学期等协议细节；无效输入可以在触发缓存、数据库或爬虫之前失败。业务层因而只接收规范化后的 `stuID + year + semester + refresh` 四元组，便于形成稳定的缓存键、刷新日志和并发去重键。

### 周数 bitset 解析

`AddClassRequest.weeks` 字段的类型是 `int64`，实际存储的是一个 **bitset**：第 $i$ 位为 1 表示第 $i$ 周有课（从第 1 位开始）。

```go
// pkg/tool/tool.go
func ParseWeeks(weeks int64) []int {
    if weeks <= 0 {
        return []int{}
    }
    var weeksList []int
    for i := 1; (1 << (i - 1)) <= weeks; i++ {
        if weeks&(1<<(i-1)) != 0 {
            weeksList = append(weeksList, i)
        }
    }
    return weeksList
}
```

例：`weeks = 0b0000_1010`（十进制 10）→ 第 2、4 周有课。

这种编码把最多 64 个布尔状态压缩进一个整数，适合协议传输和数据库索引之外的紧凑存储。需要注意“第 1 周”对应最低位，而非第 0 位；任何前端、导入脚本或迁移程序都必须遵守同一约定。`weeks <= 0` 返回空列表，因此调用方应在保存自定义课程前自行决定“未设置周次”是否允许。

### 周数格式化（FormatWeeks）

将解析出的周数列表格式化为人类可读的字符串，并自动识别单双周。

算法分三步：
1. 排序后用双指针合并连续区间，生成 `"1-3,5"` 形式的字符串。
2. 遍历所有周判断奇偶性，若全为奇数则标 `(单)`，全为偶数则标 `(双)`，混合则不加标记。
3. 拼接 `"周"` 后缀。

```go
// pkg/tool/tool.go
func FormatWeeks(weeks []int) string {
    if len(weeks) == 0 {
        return ""
    }
    sort.Ints(weeks)

    var result strings.Builder
    start := weeks[0]
    end := start
    isSingle := start%2 != 0
    isMixed := false

    for _, week := range weeks {
        if (week%2 == 0) != !isSingle {
            isMixed = true
        }
    }

    for i := 1; i < len(weeks); i++ {
        if weeks[i] == end+1 {
            end = weeks[i]
        } else {
            if start == end {
                result.WriteString(strconv.Itoa(start))
            } else {
                result.WriteString(strconv.Itoa(start) + "-" + strconv.Itoa(end))
            }
            result.WriteString(",")
            start = weeks[i]
            end = start
        }
    }

    if start == end {
        result.WriteString(strconv.Itoa(start))
    } else {
        result.WriteString(strconv.Itoa(start) + "-" + strconv.Itoa(end))
    }

    if !isMixed {
        if isSingle {
            result.WriteString("周(单)")
        } else {
            result.WriteString("周(双)")
        }
    } else {
        result.WriteString("周")
    }
    return result.String()
}
```

示例输出：`[1,3,5,7]` → `"1,3,5,7周(单)"`；`[1,2,3,4]` → `"1-4周"`。

`FormatWeeks` 会原地排序传入的切片。若上游仍需保留原始顺序，应先复制切片再格式化。单双周标识只在所有周次同为奇数或同为偶数时添加；例如 `[1, 3, 5]` 的显示文本带“单”，而 `[1, 2, 3]` 必须保持普通周次列表，避免把混合排课误导为单双周课。

### 学年学期校验（CheckSY）

```go
// pkg/tool/tool.go
func CheckSY(semester, year string) bool {
    y, err := strconv.Atoi(year)
    currentYear := time.Now().Year()
    // 年份：2006 ≤ y < currentYear+2
    tag1 := err == nil && y >= 2006 && y < currentYear+2
    // 学期：仅允许 "1"、"2"、"3"
    tag2 := semester == "1" || semester == "2" || semester == "3"
    return tag1 && tag2
}
```

这里的年份上界随系统时间变化，允许当前年和下一年，兼顾学年提前初始化的场景；下界 `2006` 则阻止明显错误的历史输入。学期值 `1`、`2`、`3` 是该服务的领域约束，不能把它简单理解成公历季度。若教务系统未来扩展学期编码，需要同时更新校验、默认值计算以及调用方的枚举定义。

## Repository 层

仓库层遵循"组合优于继承"的设计：`Cache` 和 `DAO` 各自持有一个基类（`BaseCache` / `BaseDAO`），通过**匿名字段嵌入**共享底层连接，再由 `ClassRepo` 聚合所有组件。

```
ClassRepo
├── ClassInfoCache  (embed BaseCache → redis.Cmdable)
├── StudentCourseCache
├── ClassInfoDAO    (embed BaseDAO → *gorm.DB)
└── StudentCourseDAO
```

### 缓存层（Redis）

#### 负缓存防穿透

`AddClaInfosToCache` 在 `classInfos == nil` 时写入哨兵值 `RedisNull`，并使用更短的 `blackListExpiration`。上层读到哨兵值会认为"本地无数据"，触发爬虫分支，而不会每次都打穿到数据库。

```go
// repository/cache/classInfo.go
func (c ClassInfoCache) AddClaInfosToCache(ctx context.Context, stuId, xnm, xqm string, classInfos []*model.ClassInfo) error {
    key := c.generateClassInfosKey(stuId, xnm, xqm)
    var val string
    var expire time.Duration

    if classInfos == nil {
        val = RedisNull
        expire = c.blackListExpiration   // 短过期，防止长时间屏蔽真实数据
    } else {
        valByte, err := json.Marshal(classInfos)
        if err != nil {
            return errorx.Errorf("cache.classInfo.AddClaInfosToCache: marshal key=%s, count=%d: %w", key, len(classInfos), err)
        }
        val = string(valByte)
        expire = c.classExpiration
    }

    if err := c.rdb.Set(ctx, key, val, expire).Err(); err != nil {
        return errorx.Errorf("cache.classInfo.AddClaInfosToCache: set key=%s: %w", key, err)
    }
    return nil
}
```

负缓存表达的是“在当前短 TTL 内已确认没有结果”，与 Redis 网络错误或 JSON 反序列化失败不同：后两者不能被解释为不存在，应继续走数据库或返回基础设施错误。短过期时间尤其重要——如果学生刚完成选课或爬虫刚恢复，过长的空值缓存会把真实数据误判为缺失。

#### HMGet 批量查询元数据

课程元数据（备注、是否官方）存储在 Redis Hash 中，用 `HMGet` 一次取回多条，解码失败时静默跳过，避免单条数据损坏影响整体。

Hash 的字段以课程 ID 区分，因而一次课表查询只需一次批量请求即可取回所有课程的元数据；相较于逐课程 `GET`，这能显著降低网络往返。静默跳过的代价是某条备注暂时不可见，因此应配合日志或指标监控解码失败率，避免数据格式长期漂移而没有被发现。

```go
// repository/cache/student_course.go
func (s StudentCourseCache) GetSelectClassMetaData(ctx context.Context, stuId, xnm, xqm string, claIds []string) (map[string]*model.ClassMetaData, error) {
    key := s.generateClassMetaDataKey(stuId, xnm, xqm)
    if len(claIds) == 0 {
        return map[string]*model.ClassMetaData{}, nil
    }

    vals, err := s.rdb.HMGet(ctx, key, claIds...).Result()
    if err != nil {
        return nil, errorx.Errorf("cache.studentCourse.GetSelectClassMetaData: hmget key=%s: %w", key, err)
    }

    metaDataMap := make(map[string]*model.ClassMetaData)
    for i, val := range vals {
        if val == nil {
            continue
        }
        valStr, ok := val.(string)
        if !ok {
            continue  // 静默跳过类型断言失败
        }
        var metaData model.ClassMetaData
        if err := json.Unmarshal([]byte(valStr), &metaData); err != nil {
            continue
        }
        metaDataMap[claIds[i]] = &metaData
    }
    return metaDataMap, nil
}
```

### DAO 层（GORM）

#### context 传递事务

事务通过 `context.Context` 传递，使得跨 DAO 的操作能共享同一个 `*gorm.DB`。

```go
// pkg/transaction/transaction.go
type ContextTxKey struct{}

func InTx(mysql *gorm.DB, ctx context.Context, fn func(ctx context.Context) error) error {
    return mysql.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
        ctx = context.WithValue(ctx, ContextTxKey{}, tx)
        return fn(ctx)
    })
}
```

`BaseDAO.GetDB` 优先从 context 中取出事务 `tx`，取不到才返回普通连接：

```go
// repository/dao/base.go
func (dao *BaseDAO) GetDB(ctx context.Context) *gorm.DB {
    tx, ok := ctx.Value(transaction.ContextTxKey{}).(*gorm.DB)
    if ok && tx != nil {
        return tx  // 事务分支
    }
    return dao.db.WithContext(ctx)
}
```

因此，即使 `ClassInfoDAO` 和 `StudentCourseDAO` 是独立对象，只要在同一个 `InTx` 闭包中调用，它们拿到的就是同一个 `tx`。

这种模式把事务边界显式放在用例或仓库编排处，而不是隐藏在单个 DAO 内。DAO 仍然可以独立测试和复用；编排方则负责决定哪些写操作必须原子化。使用时要把派生出的 `ctx` 继续传给所有 DAO，若误传原始 context，其中一部分写入会脱离事务，破坏原子性。

添加课程时，写入课程信息、保存学生-课程关系、检查数量上限，全部在一个事务内：

```
transaction.InTx
├── ClassInfoDAO.AddClassInfoToDB
├── StudentCourseDAO.SaveStudentAndCourseToDB
└── StudentCourseDAO.GetClassNum（超限则回滚）
```

课表刷新保存同理：

```
transaction.InTx
├── 删除旧官方课程关系
├── 批量保存 class_info（SaveClassInfosToDB）
└── 批量保存 student_course
```

#### OnConflict 三种策略

根据写入场景，GORM `Clauses(clause.OnConflict{...})` 支持三种冲突处理：

| 场景             | 策略                    | 代码片段                                                       |
| -------------- | --------------------- | ---------------------------------------------------------- |
| 课表刷新（更新核心字段）   | 按需覆盖指定列               | `DoUpdates: clause.AssignmentColumns([...])`               |
| 官方课程入库（只更新元数据） | 只覆盖 `nature`、`jxb_id` | `DoUpdates: clause.AssignmentColumns(["nature","jxb_id"])` |
| 自定义课程（幂等写入）    | 忽略冲突                  | `DoNothing: true`                                          |

> [!note] Clause 的其他用途
> `Clauses` 还可以实现行锁（`FOR UPDATE`）等逻辑，不只用于冲突处理。

#### OTel 链路追踪

GORM 与 OTel 高度集成，只要对 `*gorm.DB` 调用 `.WithContext(ctx)` 并在 ctx 中携带 span，即可自动上报 DB span：

```go
err := r.GetDB(ctx).WithContext(ctx).Table(model.ClassRefreshLogTableName)...
```

### ClassRepo 聚合

`ClassRepo` 是仓库层的对外门面，持有四个子组件，负责缓存命中/回写、事务编排和 DO↔BO 转换。

```go
type ClassRepo struct {
    ClaInfoDAO     *dao.ClassInfoDAO
    ClaInfoCache   *cache.ClassInfoCache
    StuCourseDAO   *dao.StudentCourseDAO
    StuCourseCache *cache.StudentCourseCache
    log            logger.Logger
}
```

**GetClassesFromLocal** 体现了标准的 Cache-Aside 读路径：

```
1. 读缓存
   ├── 命中 → 返回
   └── 未命中 → 读 DB
       ├── 回写缓存（best-effort，失败只打 warn）
       └── classInfos 为空 → 返回 ErrClassNotFound
2. 填充 MetaData（备注等）
3. DO → BO 转换后返回
```

**AddClass** 体现了标准的 Cache-Aside 写路径：

```
1. InTx：写 DB（AddClassInfo + SaveStudentCourse + 数量检查）
2. 失效缓存（best-effort，DB 已成功无需回滚）
```

写路径选择“先提交数据库、后失效缓存”，是为了让数据库始终作为事实来源。缓存失效失败时可能短暂读到旧值，但不应回滚已经成功的业务写入；后续 TTL 到期、下一次失效或刷新仍会修复缓存。相反，若先删缓存再写数据库，缓存重建与事务提交交错时可能把旧数据重新写回缓存。

## 一次数据变更的责任划分

| 关注点 | 主要负责层 | 失败时的处理原则 |
|---|---|---|
| 协议字段、默认学期 | gRPC / service | 尽早拒绝或补齐，不进入业务逻辑 |
| 课表读取与刷新决策 | biz/usecase | 优先可用的本地结果，必要时异步恢复 |
| 缓存命中与回写 | repository/cache | 缓存是加速层，失败不得篡改业务结果 |
| 多表持久化 | repository/dao + transaction | 整体提交或整体回滚 |
| 外部教务系统访问 | crawler / client | 记录失败并交给延迟队列重试 |

这张划分表也可作为排障起点：参数错误首先查看 service，数据丢失或不一致先检查事务边界，读到旧数据则检查缓存 TTL 与失效，爬虫失败后则转到调用链文档检查刷新日志和 Kafka 消费。

---

详细的 `GetClass` 主流程、biz 层决策逻辑和 Kafka 延迟重试机制见 [[华师匣子课表查询-GetClass调用链]]。
