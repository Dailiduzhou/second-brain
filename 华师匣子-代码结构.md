---
category: ccnubox
type: layout
topic: monorepo
architecture_types:
  - monorepo
  - workspace
status: seedling
tags:
  - ccnubox
  - ccnubox/layout
  - microservice/monorepo
---

# 华师匣子-代码结构

仓库根：`/home/mikufan/code/ccnubox-be`

## 顶层结构

```
ccnubox-be/
├── be-ccnu/              # 一站式登录
├── be-class/             # 蹭课、空闲教室
├── be-classlist/         # 课表 v1
├── be-classlist_v2/      # 课表 v2（重写版，current）
├── be-content/           # 校历、部门、banner
├── be-counter/           # 核心用户判断
├── be-elecprice/         # 电费
├── be-feed/              # 消息推送
├── be-grade/             # 成绩
├── be-library/           # 图书馆
├── be-proxy/             # IP 代理池
├── be-user/              # 用户/cookie
├── bff/                  # BFF 聚合层
├── common/               # 共享：protobuf、公共包
├── deployment/           # docker / k8s 部署
├── scripts/              # 构建/同步脚本
├── go.work               # Go workspace
├── go.work.sum
├── README.md
└── .github
```

> `go.work` 把上述每个目录声明为独立 module 一起编译，团队可以拆出子仓库也能在 monorepo 模式联调。

## 单服务 layout（Kratos 风格自研）

几乎所有服务都遵循下面的布局（参考 [[kratos layout]]）：

```
<service>/
├── cmd/                  # main.go 入口（部分服务省略，根目录直接 main.go）
├── conf/                 # proto 风格的配置定义
├── config/               # 配置结构体 + Load 逻辑
├── domain/               # 领域实体、repository interface
├── service/              # gRPC service 实现，DTO 转换
├── repository/           # repository 实现：DB、Redis、ES
├── grpc/                 # gRPC handler 注册
├── ioc/                  # 依赖注入 provider（wire）
├── pkg/                  # 业务工具（部分服务有）
├── cron/                 # 定时任务（be-content / be-elecprice / bff）
├── events/               # 事件订阅（be-classlist_v2 / be-grade / be-feed）
├── crawler/              # 爬虫（be-ccnu / be-class / be-classlist / be-grade / be-library）
├── client/               # 外部 client 封装（be-classlist_v2）
├── bizpkg/               # 业务级 pkg（be-classlist_v2）
├── tool/                 # 工具函数（be-library / common）
├── wire.go               # Wire provider 定义
├── wire_gen.go           # Wire 生成产物
├── Dockerfile
├── go.mod
├── go.sum
└── README.md             # 部分服务缺失
```

> [!info] 命名习惯
> - `conf` vs `config`：`conf` 放 proto 定义，`config` 放 Go 结构体与 Load 逻辑。
> - `domain` 严格只放 interface 和实体，不依赖具体实现。
> - `service` 是 gRPC service 的实现层，不放 HTTP handler。
> - `repository` 实现由 Wire 注入到 service。

## common 共享模块

```
common/
├── api/                  # protobuf 定义（所有服务共享）
├── bizpkg/               # 跨业务公共组件
├── pkg/                  # 通用工具
├── tool/                 # 工具包
├── go.mod
└── go.sum
```

任何服务的 proto 文件都引用 `common/api/...`，避免各服务重复定义消息。

## 脚本

```
scripts/
├── build-all.sh                  # 构建所有服务镜像
├── build-be-ccnu.sh              # 单服务构建
├── build-be-class.sh
├── build-be-classlist.sh
├── build-be-content.sh
├── build-be-counter.sh
├── build-be-elecprice.sh
├── build-be-feed.sh
├── build-be-grade.sh
├── build-be-library.sh
├── build-be-proxy.sh
├── build-be-user.sh
├── build-bff.sh
├── infra-setup.sh                # 启动 etcd/mysql/redis/kafka/es
├── service-setup.sh              # 启动所有业务服务
├── sync-config.sh                # 同步配置到部署环境
└── update-dependency.sh          # 批量更新 go.mod 依赖
```

## 构建流程

```bash
# 1. 复制 infra 配置
cp deploy/config-infra.yaml docker-config.yaml

# 2. 修改 docker-config.yaml 中的数据库/etcd 等地址

# 3. 启动基础设施
docker compose -f deploy/docker-compose-infra.yaml up -d

# 4. 构建所有服务镜像
./scripts/build-all.sh

# 5. 启动所有服务
./scripts/service-setup.sh
```

## 单服务本地调试

```bash
cd be-class
go mod tidy
go run ./cmd/class -conf ./configs/config.yaml
```

> [!tip] 一些细节
> - 12 个服务都各自有独立 `go.mod`，由 `go.work` 串起来。
> - 镜像构建上下文是仓库根，`Dockerfile` 在各服务目录下使用多阶段构建。
> - `be-classlist_v2` 是 `be-classlist` 的重写版，逐步替代 v1。
> - 部分服务（`be-class`）同时对外暴露 HTTP（18000）和 gRPC（19083）双端口。

## 跨主题链接

- [[华师匣子]] — hub
- [[华师匣子框架]] — 架构与设计
- [[go-kratos架构]] — 借鉴的 Kratos 风格
- [[kratos layout]] — 单服务 layout 模板
